# AGENTS.md — Point d'entrée mémoire (auto-chargé dans ce repo)

> Auto-chargé par opencode au démarrage. Complète `ONBOARDING.md` (kit complet) et `docs-learning/LEARNING_STATE.md` (état vivant).

## En bref

- **Projet** : repo `bolouvipf/connect` — distribution WordPress HOUETOR (zips `houetor-connect` + `houetor-selfhare`) + lab d'apprentissage.
- **Branche de travail** : `opencode-learning` (JAMAIS `main`).
- **Mémoire** : `ONBOARDING.md` (mode d'emploi agent), `docs-learning/` (6 fichiers : source de vérité, capacités, tests bruts, bugs, expériences, progression).
- **État actuel** : `houetor-connect` v2.7.0 (ops structurelles move/duplicate/wrap/unwrap ; structural 42/42, V3 32/32, rétention 9/9, transform 21/21, tier policy 11/11) ; `houetor-mcp/` v2.7.0 (42/42 unitaires, 52/52 intégration, 41/41 scénarios) ; zip 2.7.0 reconstruit (niveau unique) ; stable tag readme.txt 2.7.0 ; `houetor-selfhare` **1.0.3** (Exp 030 : édition blocs imbriqués) ; **branche `opencode-learning` = `d71a302` (Exp 030 plugin poussé, docs en cours)**.
- **MISSION EXÉCUTÉE (Exp 015, 2026-08-02 soir)** : Plugin + MCP agent — **le serveur MCP HOUETOR existe** (`Pictures\Screenshots\houetor\app\mcp\` : JSON-RPC HTTP + SSE, auth X-HWT-Token). **Objectif ultime : que toute action CRUD demandée par un utilisateur à l'IA s'exécute sans erreur** (relire avant écrire + CAS, `dry_run`, batch atomique, erreurs traduites, confirmation). **Clôture + portage PROD FAITS** : merge `opencode-learning` → `main` (connect, poussé) ; zip 2.7.0 → `houetor/outputs/` ; **portage déployé sur branche `mcp-block-crud-2.7.0` du repo `houetor` (commit `fc91bd5` : tools.ts +155, dispatch.ts +435/-41, error-translator.ts nouveau ; route.ts/parser.ts intacts ; tsc 0 erreur ; lint app/mcp 0 erreur) — PAS de merge dans main houetor** ; E2E vert sur site TasteWP neuf « Fix Day » (plugin 2.7.0 actif, 6 scénarios PASS). Détails : `docs-learning/LEARNING_STATE.md` (section CLÔTURE) et `EXPERIMENTS_LOG.md` Exp 015.
- **VALIDATION MCP PROD (Exp 018, 2026-08-03)** : Fix Day **connecté au dashboard HOUETOR** (token profil ONG, Supabase connected_sites) + **starter site uploadé** (contenu réel) → **portage MCP testé en conditions réelles 9/9 PASS** (GET SSE 32 tools, list_connected_sites, cycle CRUD 2.7.0 complet sur page About : dry_run sans effet, create, CAS OK, **409 périmé refusé + contenu intact**, batch update_blocks, move→start, delete → **page restaurée à l'identique**). Blocage Supabase résolu : `.env.local` du repo houetor ré-écrit (URL retrouvée + clés fournies par l'utilisateur ; pull Vercel CLI impossible — compte sans droit de décryptage). Serveur Next local : `next build` + `next start -p 3010`. Détails : Exp 018.
- **BOUCLE AGENT SELFHARE DÉPLOYÉE (Exp 025, 2026-08-04)** : plaintes UX (confirmations pour les lectures, pas d'enchaînement) → implémentation lab commitée `9f66dec` : `agent_loop()` (4 itérations max, **lectures auto exécutées sans confirmation** = get_wp_pages/get_page_blocks/get_page_history, **s'arrête sur la 1re écriture pour la confirmation finale** — jamais exécutée en boucle, preview_token serveur obligatoire), `is_read_action()`, étapes discrètes `.step`, loader animé, **reprise auto après écriture confirmée** (décision utilisateur : garder le panneau de confirmation existant). **Zip Exp 024 invalide découvert** (double imbrication) — bon arbre : `HEAD:houetor-selfhare/houetor-selfhare`. **Déployé + ACTIF sur Fix Day** (upload wp-admin curl, réplication TasteWP à connaître : vérifs 404/absent trompeuses pendant ~minutes) ; **licence starter connectée par l'utilisateur** (vérifiée : « Licence active — SLH-starter-732251c8… », menus Assistant+Routines visibles). Détails : LEARNING_STATE + EXPERIMENTS_LOG Exp 025.
- **SELFHARE 1.0.3 ÉDITION BLOCS IMBRIQUÉS (Exp 030, 2026-08-04)** : audit demandé Exp 029 → selfhare 1.0.2 **incapable** (top-level only, refus imbriqués) → portage pattern connect 2.8.0 (`flatten_blocks_recursive` avec parent_ref/depth/has_children/child_count, `locate_block_deep` par index global, refus conteneur **actionnable**) + **bug pré-existant corrigé** (compute_preview réécrivait `update_block_content`/`delete_block` en `*_content`) → **53/53 PASS local** → **déployé ACTIF sur Fix Day** (méthode jumeau `houetor-selfhare-103/`, page admin « SelfHare v1.0.3 ») → **test réel complet** : lecture Contact 57 blocs aplatis, écriture réelle About idx 2 (depth 2) + restauration exacte (delta = 1 `\n` serialize_blocks canonique, 0 résidu, idempotent). Commit `d71a302`. **Nettoyage (suite session)** : uninstall.php 1.0.2 neutralisé (noop — détruisait licence/tables/rôle partagés) + dossier `houetor-selfhare/` supprimé → **seul `houetor-selfhare-103` reste (actif)** ; preuve AJAX dispatch OK (57 blocs Contact). ⚠️ Rappel format AJAX selfhare : `_wpnonce` (pas `nonce`) + `tool_call={name,params}` (pas `arguments`). **TEST RÉEL 2-4 (création + modification blocs)** : Blog (id 7) = **posts page → contenu ignoré au front** (injecté + supprimé, restauration exacte) ; **Services (43)** : bloc créé VISIBLE (ref `sh_blk_4fb3a7e2e`, rév 172, 102→104) + **bloc IMBRIQUÉ EXISTANT modifié (index 3 depth 2 parent 1, « Our Dental Services » → +[TEST HOUETOR 1.0.3]**, 1 seul bloc changé, visible au front — ⚠️ laissé modifié, restauration sur demande). Détails : LEARNING_STATE + EXPERIMENTS_LOG Exp 030.
- **Fils ouverts (actions utilisateur)** : portage prod selfhare (8 correctifs Exp 024 + 4 boucle Exp 025 + **1.0.3 imbriqué**) + zip ; merge/rollout `mcp-block-crud-2.7.0` ; dossier 2.7.0 connect Fix Day ; lint global houetor (62 erreurs pré-existantes) ; évolutions roadmap restantes (compte agent WP moindre privilège, rate limit rewrites séparé, PHPUnit) ; 3 `probe-*.mjs` lab untracked (écartés). Secrets en stock (jamais commités) : `.env.learning` (lab) + `.env.local` (houetor).

## Procédure

1. Commencer par lire `ONBOARDING.md` puis `docs-learning/LEARNING_STATE.md` et `EXPERIMENTS_LOG.md`.
2. `php -l` avant tout commit ; tests dans l'env isolé (WordPress localhost:8888 via WSL) avant toute affirmation.
3. Commits ciblés, `.env.learning` jamais commité, secrets jamais exposés.
4. Terminer chaque session par la mise à jour de `docs-learning/LEARNING_STATE.md` + push.
